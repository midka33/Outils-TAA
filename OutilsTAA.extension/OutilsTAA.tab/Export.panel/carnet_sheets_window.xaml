<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mises en pages du carnet" Height="520" Width="760"
        MinHeight="360" MinWidth="560"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="CarnetTitle" Text="Carnet" FontSize="20" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" x:Name="SheetsInfo" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <DataGrid Grid.Row="2" x:Name="SheetsGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTextColumn Header="N° de mise en page" Binding="{Binding SheetNumber}" Width="180"/>
                <DataGridTextColumn Header="Nom de la mise en page" Binding="{Binding SheetName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="3" Content="Fermer" Click="Close_Click" HorizontalAlignment="Right" MinWidth="100" Margin="0,12,0,0"/>
    </Grid>
</Window>